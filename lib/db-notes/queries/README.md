# Query Contracts

Place shared SQL query contracts here when multiple apps or runtimes need to
use the same database statements.

The initial setup only establishes schema management and generated table types,
so this directory starts empty by design.
